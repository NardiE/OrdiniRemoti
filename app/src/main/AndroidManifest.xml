<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.edoardo.ordiniremoti">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".Activity.OrdiniRemoti">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="DATABASE"
            android:value="ordiniremoti.db" />
        <meta-data
            android:name="VERSION"
            android:value="4" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="com.example.edoardo.ordiniremoti" />

        <activity android:name=".Activity.SearchClient" />
        <activity android:name=".Activity.SearchArticolo" />
        <activity android:name=".Activity.TestataOrdini" />
        <activity android:name=".Activity.RigheOrdini" />
        <activity android:name=".Activity.Sincronizza" />
        <activity android:name=".Activity.ImportaAnagrafiche" />
        <activity android:name=".Activity.Impostazioni" />
        <activity android:name=".Activity.EsportaOrdini" />
        <activity android:name=".Activity.ListaOrdini"></activity>
    </application>

</manifest>